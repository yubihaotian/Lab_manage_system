<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ck.labmanagesystem.mapper.UserMapper">

    <select id="selectUserPage" resultType="com.ck.labmanagesystem.entity.User">
        SELECT * FROM users
        <where>
            <if test="query.username != null and query.username != ''">
                AND username LIKE CONCAT('%', #{query.username}, '%')
            </if>
            <if test="query.realName != null and query.realName != ''">
                AND real_name LIKE CONCAT('%', #{query.realName}, '%')
            </if>
            <if test="query.userType != null and query.userType != ''">
                AND user_type = #{query.userType}
            </if>
            <if test="query.department != null and query.department != ''">
                AND department LIKE CONCAT('%', #{query.department}, '%')
            </if>
            <if test="query.status != null">
                AND status = #{query.status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>